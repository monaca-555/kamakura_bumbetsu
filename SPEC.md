# 鎌倉市ごみ分別しらべ — 仕様書

鎌倉市が配布する「資源物とごみの分け方・出し方」冊子をもとに作成した、品目の分別区分検索と、お住まいの地区ごとの収集予定を確認できる非公式Webアプリの仕様。

- **公開URL**: https://monaca-555.github.io/kamakura_bumbetsu/
- **リポジトリ**: monaca-555/kamakura_bumbetsu
- **最終更新**: 2026-08-07

読みやすい版（デザイン付き）はこちら: https://claude.ai/code/artifact/b18941be-c163-43a1-8382-c9b58b866a8b

## 目次

1. [概要](#1-概要)
2. [画面・機能](#2-画面機能)
3. [データ仕様](#3-データ仕様)
4. [技術仕様](#4-技術仕様)
5. [制限事項・注意点](#5-制限事項注意点)
6. [公開状況](#6-公開状況)
7. [開発の経緯](#7-開発の経緯)

## 1. 概要

鎌倉市の公式ごみ分別検索が使いにくいという課題から、配布冊子の内容を自分でWebアプリ化したもの。「この品物は何ごみ？」と「今日・これから何を出せばいい？」の2つの問いに、その場でスマホから答えられることを目指す。

- 対象データの出典は鎌倉市配布冊子「資源物とごみの分け方・出し方」（令和8年4月1日改訂版）
- 個人が趣味で作成した**非公式**ツールであり、鎌倉市とは関係がない
- ビルド不要の素のHTML / CSS / JavaScriptで構成し、GitHub Pagesで無料公開

## 2. 画面・機能

### 2.1 地区選択

初回アクセス時、全画面のオーバーレイで「お住まいの地区を選んでください」と表示する。51の町名・地区名を1つのリストボックス（`<select size="8">`）から選択し、「この地区に決定」で確定するか、「あとで選ぶ」で品目検索のみの利用にスキップできる。選択結果は端末の`localStorage`に保存され、次回以降は自動的に復元される。ヘッダーの「地区を変更する」ボタンからいつでも選び直せる。

### 2.2 これから1か月の収集予定

地区決定後、ヘッダー直下に「これから1か月の収集予定」として、今日から数えて30カレンダー日分（土・日を除く、平日ベースで約22枚）を横スクロールのカードで表示する。各カードには日付・曜日と、その日に出せる資源物・ごみの区分チップを表示する。1枚目は「きょう」、2枚目（暦day+1の場合のみ）は「あした」とラベルし、それ以外は「◯曜日」と表示する。今日のカードのみ緑の太枠で強調する。土・日は収集がないため、そもそもカードを生成しない。

### 2.3 品目検索・区分フィルタ

品名を入力するとリアルタイムに絞り込み表示する（`input`イベントで反映）。検索ボタン・Enterキーでも同じ絞り込みを実行できる。全角英数は半角に正規化して比較し、該当箇所は`<mark>`でハイライトする。16種類の区分チップをタップすると、その区分だけに絞り込める（複数選択可）。品名・区分いずれも未指定の場合は結果を出さず、入力を促すメッセージを表示する。最大表示件数は120件。

### 2.4 品目詳細シート

検索結果の品目をタップすると、品名・区分（色付きチップ）・出し方のポイント・冊子の関連ページ番号を表示するボトムシートが開く。背景タップ・×ボタン・Escキーで閉じる。

### 2.5 ヘッダー表記ルール

地区決定後のタイトルは `鎌倉市ごみ分別しらべ［選択した地域名］`、サブタイトルは `［地区の正式表記］ 地区の収集情報` という規則で組み立てる。

例（鎌倉山を選択した場合）:

```
鎌倉市ごみ分別しらべ［鎌倉山］
西鎌倉・鎌倉山 地区の収集情報
```

## 3. データ仕様

### 3.1 品目データ（`data/items.js`）

冊子p.42〜65「資源物とごみの品目と分別区分（50音順索引）」の書き起こし。材質違い（金属製／プラスチック製など）は別レコードとして分割している。

| 項目 | 件数 |
|---|---|
| 品目レコード数 | 1,008件 |
| 1レコードの構造 | `[品名, 区分コード, 出し方のポイント, 関連ページ, 50音見出し]` |
| 区分コード種類 | 16種類 |

区分コード一覧:

| コード | 区分名 | 色 |
|---|---|---|
| `moyasu` | 燃やすごみ | #c96a35 |
| `moenai` | 燃えないごみ | #9c4a7a |
| `youkipura` | 容器包装プラスチック | #f3a6b8 |
| `seihinpura` | 製品プラスチック | #1f7bbf |
| `kami` | 紙類 | #c9a227 |
| `nuno` | 布類 | #9caa3a |
| `ueki` | 植木剪定材 | #2e8b57 |
| `pet` | ペットボトル | #7ec8e3 |
| `inkanbin` | 飲食用カン・ビン | #7a5ea8 |
| `shokuyu` | 使用済み食用油 | #7a6a55 |
| `kikenyugai` | 危険・有害ごみ | #d6336c |
| `kinshi` | 市で収集できないもの | #c0392b |
| `sodai` | 粗大ごみ | #2f4f6f |
| `ogatasodai` | 大型粗大ごみ | #17324a |
| `boujouitajou` | 棒状・板状等粗大ごみ | #2a6f77 |
| `tokuteisodai` | 棒状・板状等特定粗大ごみ | #1c3f5e |

### 3.2 地区別収集カレンダー（`data/districts.js`）

冊子p.1〜2「地区別収集カレンダー（令和8年4月1日から）」の書き起こし。23地区・51の町名/字名をカバーする。1地区は月〜金の各曜日に対して、「毎週出せるもの」と「月に1回、決まった巡目だけ出せるもの」の配列を持つ。

```js
{
  id: "junisho",
  label: "十二所・浄明寺・二階堂",
  areas: ["十二所", "浄明寺", "二階堂"],
  schedule: {
    mon: [ { cat: "moyasu", nth: null } ],
    tue: [ { cat: "kami", nth: null }, { cat: "nuno", nth: null },
           { cat: "moenai", nth: 1 }, { cat: "kikenyugai", nth: 1 },
           { cat: "shokuyu", nth: 1 }, { cat: "seihinpura", nth: 3 } ],
    // ...
  }
}
```

`nth: null` は毎週収集。`nth: N` は「その月でN回目にあたるその曜日」にのみ収集する品目を表す（**暦上の「第N週」とは異なる**点に注意。冊子p.2の定義に準拠）。アプリでは `Math.ceil(日付 / 7)` でその日が「その曜日の何回目か」を算出し、該当する`nth`のエントリだけを表示する。

**計算例**: 8月26日（火）の場合 → `Math.ceil(26 / 7) = 4` → 「4回目の火曜日」に該当するため、`nth: 4` の品目を収集日として表示する（8月19日は `Math.ceil(19 / 7) = 3` となり対象外）。

| 項目 | 件数 |
|---|---|
| 地区数 | 23地区 |
| 町名・字名（地区選択の選択肢数） | 51件 |

地区選択プルダウンの並び順は、冊子掲載順を基本としつつ、次の5組については「◯丁目」など番地表記のある地区名を、対応する「（未表示地区）」より先に表示するよう個別に並び替えている: 腰越／梶原／寺分／台／大船（各・一丁目〜五丁目 → 未表示地区の順）。

## 4. 技術仕様

- ビルドツール・フレームワーク不使用。素のHTML / CSS / Vanilla JavaScript（IIFE、ES5〜ES6混在）
- フォントは端末標準の日本語フォント（Hiragino Sans / Yu Gothic 等）のみ使用し、Webフォントは埋め込まない
- 状態管理は選択地区の`localStorage`保存のみ。バックエンド・APIは無し
- ホスティングはGitHub Pages（「Deploy from a branch」方式、対象ブランチのルート）

| パス | 役割 |
|---|---|
| `index.html` | ページ本体・DOM構造 |
| `css/style.css` | スタイル（ライト/ダーク自動対応） |
| `js/app.js` | 検索・フィルタ・地区選択・収集予定ロジック |
| `data/items.js` | 品目データ + 区分コード定義 |
| `data/districts.js` | 地区別収集カレンダーデータ |
| `.nojekyll` | GitHub PagesでJekyll処理を無効化 |

GitHub Pagesの配信キャッシュ対策として、`index.html`側で `js/app.js?v=YYYYMMDDx` のようにスクリプトURLへバージョンクエリを付与し、更新のたびに値を変えることでキャッシュを回避している。

## 5. 制限事項・注意点

- 非公式ツールであり、内容の正確性は保証しない。最新・正式な情報は必ず鎌倉市公式サイトで確認する旨をフッターに明記している
- 収集予定の計算は端末のローカル日時に依存する（タイムゾーンは日本国内利用を前提）
- 祝日や年末年始、悪天候等による収集日の変更・休止には対応していない（冊子の通常スケジュールのみを反映）
- 品目データは冊子からの手作業の書き起こしであり、転記誤りが含まれる可能性がある
- オフライン対応（Service Worker等によるPWA化）は現時点では未実装

## 6. 公開状況

2026-08-07時点:

- **本番** `/` — 地区選択と単日の収集案内までを備えた版
- **プレビュー** `/v4/` — 本仕様書に記載した「これから1か月の収集予定」「ヘッダー表記ルール」「地区プルダウンの並び順修正」を反映した最新版

`/v4/`の内容は動作確認済みで、本番トップページへの反映（統合）待ちの状態。

## 7. 開発の経緯

1. **初版：品目検索アプリ** — 50音順索引を書き起こし、品名検索・区分フィルタ・詳細表示ができる検索アプリとして公開
2. **GitHub Pagesでの公開** — リポジトリを公開設定に変更し、GitHub Pagesで一般公開
3. **地区選択・当日の収集案内を追加** — 地区別収集カレンダーを書き起こし、地区を選ぶと当日出せる品目が分かる機能を追加
4. **5日間 → 1か月表示への拡大** — 前夜にごみ出しを準備する利用シーンに合わせ、単日表示から5日間スワイプ表示へ、さらに約1か月分（土日除く）へ拡大。表記も「今日／明日」から「きょう／あした」に変更
5. **細部調整** — 地区プルダウンの並び順修正、見出し文言の調整、ヘッダー表記ルールの変更などを実施

---

出典：鎌倉市「資源物とごみの分け方・出し方」（令和8年4月1日改訂版）。本ドキュメントおよびアプリ本体は個人が作成した非公式のものであり、鎌倉市とは一切関係ありません。
